server:
  port: 8081

spring:
  application:
    name: inventory

  profiles:
    active: dev

  config:
    import: configserver:${config_server_url}

eureka:
  client:
    service-url:
      defaultZone: ${service_discovery_url}
    register-with-eureka: true
    fetch-registry: true




